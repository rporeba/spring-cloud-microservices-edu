server:
  port: 8091

spring:
  profiles:
    active: native
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: //https://github.com/rporeba/spring-cloud-microservices-edu